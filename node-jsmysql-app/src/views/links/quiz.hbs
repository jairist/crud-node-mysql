<div class="container p-4">
    <div class="row">
        {{#each quiz}}
           <div class="col-md-3 p-3">
               <div class="card text-center">
                  <div class="card-bady p-2">
                          <h3>{{title}}</h3>
                      </a>
                      <p class="m-2">{{description}}</p>
                      <p>{{timeago created_at}}</p>
                      <a href="/links/deletequiz/{{id}}" class="btn btn-danger">Delete Quiz</a>
                      <a href="/links/edit/{{id}}" class="btn btn-secondary">Edit</a>
                  </div> 
               </div>
           </div>
           {{else}}
           <div class="col-md-4 mx-auto">
               <div class="card card-bady text-center">
                <p>There are not quizes saved yet. </p>
                <a href="/links/addquiz">Create One!!</a>
               </div>
           </div>
        {{/each}}
    </div>
</div>